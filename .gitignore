bin/
.cache/
dist/
*.log
*.tmp
*.swp
.DS_Store
.claude/
hyprmoncfg
hyprmoncfgd
# The patterns above also match the cmd/hyprmoncfg and cmd/hyprmoncfgd
# directories, which would silently ignore their sources; re-include them (built
# binaries inside still match the name pattern).
!cmd/hyprmoncfg/
!cmd/hyprmoncfgd/
